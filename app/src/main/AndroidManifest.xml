<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" android:minSdkVersion="26" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MindMines"
        tools:targetApi="31">
        <receiver
            android:name="com.example.mindmines.services.timers.HabitStatusCheckerTimer"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.example.mindmines.MIDNIGHT_CHECK" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.example.mindmines.services.timers.DataBackupTimer"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.example.mindmines.DATA_BACKUP" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".views.habit.HabitChangeView"
            android:exported="false" />
        <activity
            android:name=".views.habit.HabitAddView"
            android:exported="false" />
        <activity
            android:name=".views.user.LoginView"
            android:exported="false" />
        <activity
            android:name=".views.user.ProfileView"
            android:exported="false" />
        <activity
            android:name=".views.habit.HabitsView"
            android:exported="false" />
        <activity
            android:name=".views.user.FriendsView"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>